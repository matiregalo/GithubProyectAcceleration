# CASOS DE PRUEBA

---

| HU | ID | Escenario Gherkin | Precondiciones | Datos de prueba | Resultado esperado | Resultado obtenido y Estado | Prioridad |
|---|---|---|---|---|---|---|---|
| HU | ID | Escenario Gherkin | Precondiciones | Datos de prueba | Resultado esperado | Resultado obtenido y Estado | Prioridad |
|---|---|---|---|---|---|---|---|
| HU-01 | TC-001 | **Given** un administrador con sesión activa<br>**When** cierra su sesión desde el Dashboard<br>**Then** el sistema finaliza la sesión y cualquier intento de acceder nuevamente es rechazado | Usuario con sesión autenticada activa | Sesión válida previamente iniciada | Sesión cerrada; acceso posterior denegado | Resultado obtenido: Sesión cerrada; acceso posterior denegado; Estado: Pasó | Crítico |
| HU-03 | TC-003 | **Given** un STORE_ADMIN autenticado cuyo ecommerce no tiene usuarios registrados todavía<br>**When** consulta el listado de usuarios<br>**Then** el sistema muestra una lista vacía sin errores | STORE_ADMIN autenticado; ecommerce sin usuarios creados | ecommerceId válido con 0 usuarios | Lista vacía visible sin mensajes de error | Resultado obtenido: Lista vacía visible sin mensajes de error; Estado: Pasó | Medio |
| HU-03; HU-16 | TC-004 | **Given** un "&lt;rol&gt;" autenticado con un usuario "&lt;usuario_objetivo&gt;" en su ecommerce<br>**When** actualiza el nombre a "&lt;nuevo_nombre&gt;" y el correo a "&lt;nuevo_email&gt;" de ese usuario<br>**Then** el perfil refleja los nuevos datos y la vinculación con la tienda permanece intacta<br>*(Scenario Outline con Examples: STORE_ADMIN / usuario_estandar / Nuevo Nombre / nuevo@tiendaX.com; SUPER_ADMIN / store_admin / Admin Actualizado / admin.nuevo@tiendaX.com)* | Iter 1: STORE_ADMIN autenticado; usuario estándar en su ecommerce; Iter 2: SUPER_ADMIN autenticado; STORE_ADMIN existente vinculado al ecommerce X | Iter 1: rol=STORE_ADMIN; usuario_objetivo=usuario_estandar; nuevo_nombre="Nuevo Nombre"; nuevo_email="nuevo@tiendaX.com"; Iter 2: rol=SUPER_ADMIN; usuario_objetivo=store_admin; nuevo_nombre="Admin Actualizado"; nuevo_email="admin.nuevo@tiendaX.com" | Iter 1: datos del perfil actualizados; usuario sigue vinculado a la misma tienda; Iter 2: datos del STORE_ADMIN actualizados; vinculación con ecommerce X inalterada | Resultado obtenido: Iter 1: datos del perfil actualizados; usuario sigue vinculado a la misma tienda; Iter 2: datos del STORE_ADMIN actualizados; vinculación con ecommerce X inalterada; Estado: Pasó | Alto |
| HU-04 | TC-005 | **Given** un usuario estándar autenticado con perfil existente<br>**When** actualiza su correo a "a@b.c"<br>**Then** el perfil refleja el nuevo correo y la vinculación con la tienda permanece | Usuario USER autenticado; perfil existente | email="a@b.c" (formato mínimo válido) | Perfil actualizado; correo visible; vinculación con el ecommerce inalterada | Resultado obtenido: Perfil actualizado; correo visible; vinculación con el ecommerce inalterada; Estado: Pasó | Medio |
| HU-04 | TC-005-B | **Given** un usuario estándar autenticado con perfil existente<br>**When** actualiza su correo a ""<br>**Then** el sistema informa que el correo es un dato requerido y no aplica los cambios | Usuario USER autenticado; perfil existente | email="" (campo vacío) | Actualización rechazada; campo correo marcado como requerido; perfil sin cambios | Resultado obtenido: Actualización rechazada; campo correo marcado como requerido; perfil sin cambios; Estado: Pasó | Medio |
| HU-05 | TC-009 | **Given** un "&lt;actor&gt;" que intenta generar una clave para "&lt;tienda&gt;"<br>**When** intenta la operación de generación de clave<br>**Then** &lt;resultado_esperado&gt;<br>*(Scenario Outline con Examples: Super Admin / tienda inexistente → rechazada por tienda no encontrada; usuario sin sesión / tienda válida → rechazada por falta de autenticación; STORE_ADMIN / tienda válida → rechazada por permisos insuficientes)* | Tres actores con distintos contextos (solo casos negativos) | Tres combinaciones de rol y estado del ecommerce | Cada actor recibe rechazo apropiado según su situación: tienda no encontrada, falta de autenticación o permisos insuficientes | Resultado obtenido: Cada actor recibe rechazo apropiado según su situación: tienda no encontrada, falta de autenticación o permisos insuficientes; Estado: Pasó | Crítico |
| HU-09 | TC-010 | **Given** el administrador configura un tope de descuento positivo y asigna prioridades únicas a cada tipo de regla<br>**When** guarda la configuración<br>**Then** la configuración queda activa y el motor la aplica para resolver concurrencia entre reglas | Usuario LOYALTY autenticado; tipos de descuento disponibles | tope=20%; prioridades: Temporada=1, Fidelidad=2, TipoProducto=3 | Configuración guardada y activa; motor aplica el tope y el orden de prioridades definido | Resultado obtenido: Configuración guardada y activa; motor aplica el tope y el orden de prioridades definido; Estado: Pasó | Crítico |
| HU-09 | TC-011 | **Given** el administrador configura un tope de descuento con el valor mínimo permitido<br>**When** guarda la configuración con ese valor<br>**Then** el sistema lo acepta y registra la configuración correctamente | Usuario LOYALTY autenticado | tope=0.01%; prioridades únicas | Configuración guardada con el tope mínimo válido | Resultado obtenido: Configuración guardada con el tope mínimo válido; Estado: Pasó | Medio |
| HU-09 | TC-012 | **Given** cuatro combinaciones de tope y prioridades: tope válido con prioridades únicas, tope cero con prioridades únicas, tope válido con prioridades duplicadas, tope negativo con prioridades duplicadas<br>**When** se intenta guardar cada combinación<br>**Then** solo la primera es aceptada; las demás son rechazadas con el aviso correspondiente a cada problema | Usuario LOYALTY autenticado; configuración previa válida | Cuatro combinaciones de tope y prioridades | Solo la combinación con tope válido y prioridades únicas es guardada; los demás casos producen rechazo con mensajes específicos | Resultado obtenido: Solo la combinación con tope válido y prioridades únicas es guardada; los demás casos producen rechazo con mensajes específicos; Estado: Pasó | Crítico |
| HU-12 | TC-013 | **Given** un usuario LOYALTY autenticado con transacciones registradas en los últimos siete días<br>**When** consulta el historial de descuentos aplicados filtrando por esa semana<br>**Then** el sistema muestra el listado de transacciones con su identificador, fecha, reglas aplicadas, descuento calculado y descuento final | Usuario LOYALTY autenticado; transacciones de la semana disponibles | filtro: últimos 7 días | Lista de transacciones visible con todos los campos requeridos; transacciones con descuento recortado por tope identificadas visualmente | Resultado obtenido: Lista de transacciones visible con todos los campos requeridos; transacciones con descuento recortado por tope identificadas visualmente; Estado: Pasó | Crítico |
| HU-12 | TC-014 | **Given** existen transacciones en las que el descuento fue reducido por el tope máximo configurado<br>**When** el usuario LOYALTY visualiza esas transacciones en el historial<br>**Then** el sistema las resalta claramente y no expone ningún dato personal del comprador | Usuario LOYALTY autenticado; transacciones con descuento truncado disponibles | Transacciones con descuento recortado por tope en el período | Transacciones recortadas identificadas visualmente; sin nombre, correo ni datos personales del comprador final | Resultado obtenido: Transacciones recortadas identificadas visualmente; sin nombre, correo ni datos personales del comprador final; Estado: Pasó | Crítico |
| HU-12 | TC-015 | **Given** el usuario LOYALTY aplica un filtro de historial que incluye exactamente el séptimo día de retención<br>**When** el sistema procesa la consulta<br>**Then** los registros del séptimo día son incluidos en el resultado | Usuario LOYALTY autenticado | fechaDesde=hoy-7días exactos | Registros del séptimo día presentes en el historial | Resultado obtenido: Registros del séptimo día presentes en el historial; Estado: Pasó | Medio |
| HU-12 | TC-016 | **Given** el usuario LOYALTY aplica un filtro que solicita datos más allá del período de retención de siete días<br>**When** el sistema procesa la consulta<br>**Then** los registros anteriores al límite de retención no aparecen en el resultado | Usuario LOYALTY autenticado | fechaDesde=hoy-8días | Registros de hace más de 7 días excluidos del historial | Resultado obtenido: Registros de hace más de 7 días excluidos del historial; Estado: Pasó | Medio |
| HU-12 | TC-017 | **Given** tres escenarios: usuario LOYALTY con transacciones en el período, usuario LOYALTY sin transacciones en el período, usuario sin sesión activa<br>**When** cada uno consulta el historial<br>**Then** el autenticado con transacciones ve el listado, el autenticado sin transacciones ve lista vacía y el no autenticado es rechazado | Tres actores distintos | Tres combinaciones de autenticación y disponibilidad de transacciones | Con transacciones: listado visible; sin transacciones: lista vacía; sin sesión: acceso denegado | Resultado obtenido: Con transacciones: listado visible; sin transacciones: lista vacía; sin sesión: acceso denegado; Estado: Pasó | Alto |
| HU-13 | TC-018 | **Given** el Super Admin accede al panel de auditoría de un ecommerce con cambios registrados<br>**When** consulta el historial filtrando por tipo de regla Temporada<br>**Then** el sistema muestra una tabla cronológica con el usuario, la fecha, la regla afectada y los valores anterior y nuevo | SUPER_ADMIN autenticado; cambios registrados en audit_logs del ecommerce | filtro tipo=Temporada; ecommerceId válido | Tabla de auditoría visible con todos los campos: usuario, fecha, regla afectada, valor anterior, valor nuevo | Resultado obtenido: Tabla de auditoría visible con todos los campos: usuario, fecha, regla afectada, valor anterior, valor nuevo; Estado: Pasó | Crítico |
| HU-13 | TC-019 | **Given** cuatro actores: Super Admin con registros, Super Admin sin registros, STORE_ADMIN con registros, usuario sin sesión<br>**When** cada uno accede al panel de auditoría<br>**Then** el Super Admin ve el historial (con o sin registros), el STORE_ADMIN es rechazado y el usuario sin sesión también | Cuatro actores con distintos contextos | Cuatro combinaciones de rol y disponibilidad de registros | SUPER_ADMIN con registros: tabla visible; SUPER_ADMIN sin registros: lista vacía; STORE_ADMIN: acceso denegado; sin sesión: acceso denegado | Resultado obtenido: SUPER_ADMIN con registros: tabla visible; SUPER_ADMIN sin registros: lista vacía; STORE_ADMIN: acceso denegado; sin sesión: acceso denegado; Estado: Pasó | Crítico |
| HU-16 | TC-020 | **Given** el Super Admin consulta la lista global de ecommerces cuando no hay ninguna tienda registrada<br>**When** consulta la lista global de ecommerces<br>**Then** muestra una lista vacía de ecommerces sin errores | SUPER_ADMIN autenticado; sistema sin ecommerces registrados | Consulta global con sistema vacío | Lista vacía de ecommerces visible; sin errores | Resultado obtenido: Lista vacía de ecommerces visible; sin errores; Estado: Pasó | Medio |
| HU-16 | TC-021 | **Given** el Super Admin autenticado tiene ecommerces y usuarios existentes<br>**When** ejecuta operaciones de crear STORE_ADMIN, listar ecommerces, eliminar STORE_ADMIN y reasignar usuario<br>**Then** todas las operaciones son realizadas exitosamente sin restricción de ecommerce | SUPER_ADMIN autenticado; ecommerces y usuarios existentes | Cuatro operaciones sobre usuarios y ecommerces distintos | Todas las operaciones realizadas exitosamente; sin restricción de ecommerce | Resultado obtenido: Todas las operaciones realizadas exitosamente; sin restricción de ecommerce; Estado: Pasó | Crítico |

| HU-01 | TC-022 | **Given** el servicio loyalty-admin está activo en `http://localhost:8081`<br>**When** se envía `POST /api/v1/auth/login` con credenciales válidas<br>**Then** el sistema retorna HTTP 200 con un token JWT en la respuesta | Servicio loyalty-admin activo; usuario registrado | username="admin"; password="Pass123!" | 1. Enviar POST /api/v1/auth/login con credenciales válidas<br>2. Verificar HTTP 200<br>3. Verificar que la respuesta contiene el campo `token` | HTTP 200; campo `token` presente en la respuesta; token con formato JWT válido | Resultado obtenido: HTTP 200; campo `token` presente en la respuesta; token con formato JWT válido; Estado: Pasó | Crítico |
| HU-01 | TC-023 | **Given** el servicio loyalty-admin está activo<br>**When** se envían tres combinaciones de credenciales de admin al endpoint `POST /api/v1/auth/login`:  contraseña errónea, usuario vacío, ambos vacíos<br>**Then** retornan HTTP 400 o HTTP 401 | Servicio loyalty-admin activo; usuario registrado | Iter 1: password incorrecta; Iter 2: username=""; Iter 3: ambos vacíos | 1. Ejecutar cada iteración<br>2. Verificar HTTP 401 para Iter 1<br>4. Verificar HTTP 400 para Iter 2 e Iter 3 | Iter 1: HTTP 401; Iter 2-3: HTTP 400; ningún caso negativo expone token | Resultado obtenido: Iter 1: HTTP 401; Iter 2-3: HTTP 400; ningún caso negativo expone token; Estado: Pasó | Crítico |
| HU-01 | TC-024 | **Given** un usuario con sesión activa y token JWT válido<br>**When** se envía `POST /api/v1/auth/logout` con ese token en el header `Authorization`<br>**Then** el sistema retorna HTTP 204 | Token JWT válido; usuario autenticado | header Authorization: Bearer <token válido> | 1. Enviar POST /api/v1/auth/logout con el token<br>2. Verificar HTTP 204<br> | HTTP 204 en logout | Resultado obtenido: HTTP 204 en logout; Estado: Pasó | Crítico |
| HU-02 | TC-025 | **Given** un administrador autenticado<br>**When** se envía `POST /api/v1/users` en 2 iteraciones:  Iter 1 con ecommerceId inexistente en sistema; Iter 2 con username vacío<br>**Then**  Iter 1 retorna HTTP 404; Iter 2 retorna HTTP 400 indicando campo requerido | ADMIN autenticado; Iter 1: ecommerceId inexistente; Iter 2: ecommerce válido | Iter 1: ecommerceId="ID-INEXISTENTE"; demás campos válidos; Iter 2: username="" (vacío); demás campos válidos | 1. Iter 1: Enviar POST con ecommerceId inexistente → verificar HTTP 404 y mensaje de ecommerce no encontrado<br>2. Iter 2: Enviar POST sin username → verificar HTTP 400 y campo faltante identificado | Iter 1: HTTP 404; mensaje de ecommerce no encontrado; sin usuario creado; Iter 2: HTTP 400; mensaje indica `username` requerido; sin usuario creado | Iter 1: HTTP 404; mensaje de ecommerce no encontrado; sin usuario creado; Iter 2: HTTP 400; mensaje indica `username` requerido; sin usuario creado; Estado: Pasó | Crítico |
| HU-02 | TC-026 | **Given** un administrador autenticado con usuarios registrados en su ecommerce<br>**When** se envía `GET /api/v1/users?ecommerceId={id}` con el identificador del ecommerce<br>**Then** el sistema retorna HTTP 200 con la lista de usuarios que incluye nombre, rol, nombre del rol, email y fecha de creación | ADMIN autenticado; al menos 2 usuarios en su ecommerce | ecommerceId válido con usuarios existentes | 1. Enviar GET /api/v1/users con el ecommerceId<br>2. Verificar HTTP 200<br>3. Verificar que cada usuario tiene username, role, email y createdAt | HTTP 200; lista con todos los campos requeridos por usuario | Resultado obtenido:HTTP 200; lista con todos los campos requeridos por usuario; Estado: Pasó | Alto |
| HU-02 | TC-027 | **Given** un administrador autenticado con un usuario estándar en su ecommerce<br>**When** se envía `DELETE /api/v1/users/{userId}` con el identificador del usuario<br>**Then** el sistema retorna HTTP 204 y el usuario no puede autenticarse posteriormente | ADMIN autenticado; usuario estándar existente en su ecommerce | userId válido del usuario a eliminar | 1. Enviar DELETE /api/v1/users/{userId}<br>2. Verificar HTTP 204<br>3. Intentar login con las credenciales del usuario eliminado<br>4. Verificar HTTP 401 en el intento | HTTP 204 en eliminación; HTTP 401 en el intento de login posterior | Resultado obtenido: HTTP 204 en eliminación; HTTP 401 en el intento de login posterior; Estado: Pasó | Alto |
| HU-02 | TC-028 | **Given** cuatro actores: ADMIN con ecommerce válido, ADMIN con ecommerce inexistente, USER autenticado, usuarion sin sesión<br>**When** cada uno envía `POST /api/v1/users`<br>**Then** solo el ADMIN con ecommerce válido recibe HTTP 201; los demás reciben el código de error correspondiente | Cuatro actores con distintos contextos | Cuatro combinaciones de rol y estado del ecommerce | 1. Ejecutar cada solicitud con su contexto<br>2. Verificar HTTP por cada actor | ADMIN+ecommerce válido: HTTP 201; ADMIN+ecommerce inexistente: HTTP 404; USER: HTTP 403; sin sesión: HTTP 401 | Resultado obtenido: ADMIN+ecommerce válido: HTTP 201; ADMIN+ecommerce inexistente: HTTP 404; USER: HTTP 403; sin sesión: HTTP 401; Estado: Pasó | Crítico |
| HU-03 | TC-029 | **Given** un STORE_ADMIN autenticado en el ecommerce X<br>**When** se envía `POST /api/v1/users` con datos válidos y `ecommerceId=X`<br>**Then** el sistema retorna HTTP 201 y el usuario queda vinculado al ecommerce X | STORE_ADMIN autenticado; ecommerce X registrado | username="usr.std"; email="std@tiendaX.com"; password="Pass123!"; roleId= 10201221; ecommerceId=X | 1. Enviar POST /api/v1/users con token STORE_ADMIN y ecommerceId propio<br>2. Verificar HTTP 201<br>3. Verificar usuario creado en ecommerce X | HTTP 201; usuario vinculado a ecommerce X | Resultado obtenido: HTTP 201; usuario vinculado a ecommerce X; Estado: Pasó | Crítico |
| HU-03 | TC-030 | **Given** un STORE_ADMIN autenticado en el ecommerce X<br>**When** se envía `POST /api/v1/users` con `ecommerceId=Y` (ecommerce distinto al suyo)<br>**Then** el sistema retorna HTTP 403 | STORE_ADMIN autenticado en ecommerce X; ecommerce Y existente | ecommerceId=Y (distinto al del STORE_ADMIN) | 1. Enviar POST /api/v1/users con ecommerceId=Y<br>2. Verificar HTTP 403<br>3. Verificar mensaje de permisos insuficientes | HTTP 403 Forbidden; sin usuario creado en ecommerce Y | Resultado obtenido: HTTP 403 Forbidden; sin usuario creado en ecommerce Y; Estado: Pasó | Crítico |
| HU-03 | TC-031 | **Given** tres actores con distintos contextos intentan `GET /api/v1/users?ecommerceId={id}`<br>**When** cada uno realiza la consulta<br>**Then** solo el STORE_ADMIN sobre su propio ecommerce recibe HTTP 200; los demás reciben HTTP 403 o HTTP 401 | Tres actores: STORE_ADMIN sobre su ecommerce, STORE_ADMIN sobre otro ecommerce, sin sesión | Tres combinaciones de rol y ecommerce consultado | 1. Ejecutar cada solicitud<br>2. Verificar HTTP por cada actor | STORE_ADMIN propio: HTTP 200; STORE_ADMIN ajeno: HTTP 403; sin sesión: HTTP 401 | Resultado obtenido: STORE_ADMIN propio: HTTP 200; STORE_ADMIN ajeno: HTTP 403; sin sesión: HTTP 401; Estado: Pasó | Crítico |
| HU-04 | TC-032 | **Given** un usuario con rol USER autenticado<br>**When** se envía `GET /api/v1/users/me` para consultar su propio perfil<br>**Then** el sistema retorna HTTP 200 con sus datos personales sin exponer datos de otros usuarios | USER autenticado | header Authorization: Bearer <token USER> | 1. Enviar GET /api/v1/users/me con token USER<br>2. Verificar HTTP 200<br>3. Verificar que la respuesta contiene sólo datos del propio usuario | HTTP 200; datos del propio usuario; sin datos de otros usuarios | Resultado obtenido: HTTP 200; datos del propio usuario; sin datos de otros usuarios; Estado: Pasó | Alto |
| HU-04 | TC-033 | **Given** un usuario con rol USER autenticado<br>**When** intenta realizar una operacion reservada a roles superiores: `POST /api/v1/users`<br>**Then** retorna HTTP 403 | USER autenticado | token USER; endpoint de gestión | 1. Enviar POST /api/v1/users<br>2. Verificar HTTP 403 | HTTP 403 ; sin datos sensibles expuestos | Resultado obtenido: HTTP 403 ; sin datos sensibles expuestos; Estado: Pasó | Alto |
| HU-04 | TC-034 | **Given** un usuario con rol USER autenticado<br>**When** envía `PUT /api/v1/users/me` con email válido mínimo `"a@b.c"`<br>**Then** el sistema retorna HTTP 200 y el perfil refleja el nuevo email | USER autenticado; perfil existente | email="a@b.c" (formato mínimo válido) | 1. Enviar PUT /api/v1/users/me con nuevo email<br>2. Verificar HTTP 200<br>3. Verificar email actualizado en GET /api/v1/users/me | HTTP 200; email actualizado en el perfil | Resultado obtenido: HTTP 200; email actualizado en el perfil ; Estado: Pasó | Medio |
| HU-05 | TC-035 | **Given** el Engine Service está activo en `http://localhost:8082`<br>**When** se envía `POST /api/v1/engine/calculate` en dos iteraciones: Iter 1 con API Key válida sincronizada en caché Caffeine; Iter 2 con API Key inválida o revocada<br>**Then** Iter 1 retorna HTTP 200 procesando la solicitud; Iter 2 retorna HTTP 401 rechazando el acceso | Engine Service activo; Iter 1: API Key válida en caché Caffeine; Iter 2: API Key revocada o inexistente en caché | Iter 1: header x-api-key con UUID v4 válido registrado; Iter 2: header x-api-key="INVALID-KEY-000" | 1. Iter 1: Enviar POST al Engine con API Key válida → verificar HTTP 200 y campos de descuento calculado en respuesta<br>2. Iter 2: Enviar POST al Engine con API Key inválida → verificar HTTP 401 y ausencia de datos de descuento | Iter 1: HTTP 200; solicitud aceptada; respuesta con cálculo de descuento; Iter 2: HTTP 401 Unauthorized; acceso denegado; sin cálculo realizado | Resultado obtenido: Iter 1: HTTP 200; solicitud aceptada; respuesta con cálculo de descuento; Iter 2: HTTP 401 Unauthorized; acceso denegado; sin cálculo realizado; Estado: Pasó | Crítico |
| HU-06 | TC-036 | **Given** un usuario LOYALTY autenticado y no existen reglas de temporada en conflicto para el período<br>**When** se envía `POST /api/v1/seasonal-rules` con nombre, fechas válidas y descuento=20%<br>**Then** el sistema retorna HTTP 201 y la regla queda activa | LOYALTY autenticado; sin reglas en conflicto para el período | { "nombre": "Black Friday 2026", "fechaInicio": "2026-11-27", "fechaFin": "2026-11-30", "descuento": 20 } | 1. Enviar POST /api/v1/seasonal-rules con datos válidos<br>2. Verificar HTTP 201<br>3. Verificar que la regla aparece en GET /seasonal-rules con estado activo | HTTP 201; regla creada con estado activo; visible en listado | Resultado obtenido: HTTP 201; id con formato UUID; nombre="Black Friday 2026"; descuento=20%; regla visible en listado GET; Estado: Pasó | Crítico |
| HU-06 | TC-038 | **Given** existe una regla de temporada registrada<br>**When** se envía `PUT /api/v1/seasonal-rules/{id}` con nuevas fechas válidas y descuento=25%<br>**Then** el sistema retorna HTTP 200 y la regla queda actualizada | LOYALTY autenticado; regla de temporada existente | nuevas fechas: inicio="2026-06-01"; fin="2026-06-30"; descuento=25 | 1. Enviar PUT /api/v1/seasonal-rules/{id} con valores nuevos<br>2. Verificar HTTP 200<br>3. Verificar datos actualizados en GET /seasonal-rules/{id} | HTTP 200; regla actualizada con nuevos valores | Resultado obtenido: HTTP 200; descuento=25%; nombre="Regla actualizada TC-038"; confirmado en GET posterior; Estado: Pasó | Crítico |
| HU-06 | TC-039 | **Given** existe una regla de temporada registrada<br>**When** se envía `DELETE /api/v1/seasonal-rules/{id}`<br>**Then** el sistema retorna HTTP 204 y la regla queda inactiva | LOYALTY autenticado; regla existente | id de la regla a eliminar | 1. Enviar DELETE /api/v1/seasonal-rules/{id}<br>2. Verificar HTTP 204<br>3. Verificar isActive==false en GET /seasonal-rules/{id} | HTTP 204; regla con isActive=false en consulta posterior | Resultado obtenido: HTTP 204 en eliminación; GET posterior retorna isActive=false; Estado: Pasó | Alto |
| HU-06 | TC-040 | **Given** tres combinaciones: datos válidos, descuento negativo inválido, edición de regla inexistente<br>**When** se intenta cada operación sobre `POST` o `PUT /api/v1/seasonal-rules`<br>**Then** solo la primera retorna HTTP 201; las demás retornan el código de error correspondiente | LOYALTY autenticado | Tres combinaciones de datos y valores de descuento | 1. Ejecutar cada operación<br>2. Verificar HTTP por cada caso | Caso 1: HTTP 201; Caso 2: HTTP 400; Caso 3: HTTP 404 | Resultado obtenido: Caso 1 (datos válidos): HTTP 201; Caso 2 (descuento negativo): HTTP 400; Caso 3 (regla inexistente): HTTP 404; Estado: Pasó | Alto |
| HU-10 | TC-049 | **Given** el Motor está activo y la API Key es válida<br>**When** se envían cinco payloads con campos obligatorios omitidos o inválidos: sin orderCount, sin ecommerceId, payload vacío, orderCount=null, orderCount=-1<br>**Then** el sistema retorna HTTP 400 en los cinco casos | Motor activo; API Key válida; cinco payloads incompletos o inválidos | Iter 1: sin orderCount; Iter 2: sin ecommerceId; Iter 3: {}; Iter 4: orderCount=null; Iter 5: orderCount=-1 | 1. Enviar POST por cada iteración<br>2. Verificar HTTP 400 en cada caso | HTTP 400 en las cinco iteraciones | Resultado obtenido: HTTP 400 en las 5 iteraciones (sin orderCount, sin ecommerceId, vacío, null, negativo); Estado: Pasó | Crítico |
| HU-11 | TC-053 | **Given** el Motor está activo con API Key válida y reglas vigentes con tope máximo configurado<br>**When** se envía `POST /api/v1/engine/calculate` con items=[{ productId, quantity: 2, unitPrice: 100.00, category: "GENERAL" }] y cliente con alto historial (totalSpent=5000, orderCount=50, membershipDays=730)<br>**Then** el sistema retorna HTTP 200 con subtotalAmount=200.00, finalAmount ≤ subtotalAmount, campos discountApplied, wasCapped y appliedRules presentes | Motor activo; API Key válida; reglas de descuento y tope configurados | items=[{ productId: "PROD-CAP-1", quantity: 2, unitPrice: 100.00, category: "GENERAL" }]; totalSpent=5000; orderCount=50; membershipDays=730 | 1. Enviar POST /api/v1/engine/calculate con API Key válida y payload completo<br>2. Verificar HTTP 200<br>3. Verificar subtotalAmount==200.00<br>4. Verificar finalAmount ≤ subtotalAmount y ≥ 0<br>5. Verificar campos discountApplied, wasCapped (boolean) y appliedRules (array) presentes | HTTP 200; subtotalAmount=200.00; finalAmount ≤ subtotalAmount; discountApplied y wasCapped presentes; appliedRules es array | Resultado obtenido: HTTP 200; subtotalAmount=200.00; finalAmount ≤ subtotalAmount; campos discountApplied, wasCapped y appliedRules presentes; Estado: Pasó | Crítico |
| HU-11 | TC-054 | **Given** el Motor está activo con API Key válida y cliente con bajo historial<br>**When** se envía `POST /api/v1/engine/calculate` con items=[{ productId, quantity: 1, unitPrice: 100.00, category: "GENERAL" }] y cliente con poco historial (totalSpent=100, orderCount=2, membershipDays=30)<br>**Then** el sistema retorna HTTP 200 con subtotalAmount=100.00, finalAmount ≤ 100.00 y customerTier presente | Motor activo; API Key válida; reglas y tope configurados | items=[{ productId: "PROD-EXACT", quantity: 1, unitPrice: 100.00, category: "GENERAL" }]; totalSpent=100; orderCount=2; membershipDays=30 | 1. Enviar POST /api/v1/engine/calculate con API Key válida y payload con cliente de bajo historial<br>2. Verificar HTTP 200<br>3. Verificar subtotalAmount==100.00<br>4. Verificar finalAmount ≤ 100.00 y ≥ 0<br>5. Verificar customerTier y discountApplied presentes | HTTP 200; subtotalAmount=100.00; finalAmount ≤ 100.00; customerTier presente; discountApplied presente | Resultado obtenido: HTTP 200; subtotalAmount=100.00; finalAmount ≤ 100.00; customerTier presente; Estado: Pasó | Crítico |
| HU-11 | TC-055 | **Given** el Motor está activo y la API Key es válida<br>**When** se envía `POST /api/v1/engine/calculate` con items=[{ productId: "PROD-MIN", quantity: 1, unitPrice: 0.01, category: "GENERAL" }]<br>**Then** el sistema retorna HTTP 200 con subtotalAmount=0.01 y finalAmount entre 0 y 0.01 | Motor activo; API Key válida | items=[{ productId: "PROD-MIN", quantity: 1, unitPrice: 0.01, category: "GENERAL" }] | 1. Enviar POST /api/v1/engine/calculate con ítem unitPrice=0.01<br>2. Verificar HTTP 200<br>3. Verificar subtotalAmount==0.01<br>4. Verificar finalAmount ≥ 0 y ≤ 0.01 | HTTP 200; subtotalAmount=0.01; finalAmount entre 0 y 0.01 | Resultado obtenido: HTTP 200; subtotalAmount=0.01; finalAmount calculado correctamente; Estado: Pasó | Medio |
| HU-11 | TC-056 | **Given** el Motor está activo y la API Key es válida<br>**When** se envía `POST /api/v1/engine/calculate` con el payload `{ "items": [{ "unitPrice": -1.00, "quantity": 1 }] }`<br>**Then** el sistema retorna HTTP 400 indicando que el precio del ítem debe ser mayor a cero | Motor activo; API Key válida | items=[{ productId: "PROD-NEG", unitPrice: -1.00, quantity: 1, category: "GENERAL" }] | 1. Enviar POST con ítem unitPrice=-1.00<br>2. Verificar HTTP 400 | HTTP 400; item con precio negativo rechazado | Resultado obtenido: HTTP 400; item con precio negativo rechazado; Estado: Pasó | Alto |
| HU-11 | TC-057 | **Given** cuatro combinaciones de condiciones de solicitud al Motor<br>**When** se ejecuta cada solicitud<br>**Then** el sistema retorna el código HTTP y resultado correspondiente | R1: API Key válida + carrito con 2 items unitPrice=100 + cliente existente; R2: sin API Key + carrito válido; R3: API Key válida + sin campo items; R4: API Key válida + cliente nuevo (totalSpent=0, orderCount=0) + 1 item unitPrice=200 | R1: items=[{ unitPrice:100, qty:2 }]; R2: sin header x-api-key; R3: payload sin campo items; R4: items=[{ unitPrice:200, qty:1 }], customerId="NEW-CUST-NO-DISCOUNT" | 1. R1: Enviar POST con API Key válida y carrito → verificar HTTP 200, subtotalAmount=200.00, finalAmount ≤ subtotalAmount<br>2. R2: Enviar POST sin x-api-key → verificar HTTP 401<br>3. R3: Enviar POST sin campo items → verificar HTTP 400<br>4. R4: Enviar POST con cliente nuevo → verificar HTTP 200, subtotalAmount=200.00, finalAmount ≤ subtotalAmount | R1: HTTP 200; subtotalAmount=200.00; finalAmount ≤ subtotalAmount; R2: HTTP 401; R3: HTTP 400; R4: HTTP 200; subtotalAmount=200.00; finalAmount ≤ subtotalAmount | Resultado obtenido: R1: HTTP 200 subtotalAmount=200.00 con descuento; R2: HTTP 401; R3: HTTP 400; R4: HTTP 200 subtotalAmount=200.00 cliente nuevo; Estado: Pasó | Crítico |
| HU-09 | TC-058 | **Given** cuatro combinaciones de configuración de descuento enviadas al endpoint<br>**And** se usa token de ADMIN en los cuatro casos<br>**When** se envía `POST /api/v1/discount-config` con cada combinación<br>**Then** el sistema retorna HTTP 201 (o 500 si ya existe) para el caso válido; los demás retornan HTTP 400 o 500 | ADMIN autenticado; configuración previa válida | R1: config válida completa; R2: maxDiscountLimit=0; R3: ecommerceId inválido; R4: maxDiscountLimit=-5 | 1. Ejecutar R1 y verificar HTTP 201 o existencia previa<br>2. Ejecutar R2 y verificar HTTP 400<br>3. Ejecutar R3 y verificar HTTP 500 por formato inválido<br>4. Ejecutar R4 y verificar HTTP 400 | R1: HTTP 201/existente + GET 200 con maxDiscountLimit; R2: HTTP 400; R3: HTTP 500; R4: HTTP 400 | Resultado obtenido: R1: config válida creada o ya existente, GET confirma maxDiscountLimit; R2: HTTP 400 (tope=0); R3: HTTP 500 (UUID inválido); R4: HTTP 400 (tope negativo); Estado: Pasó | Crítico |
| HU-14 | TC-059 | **Given** un STORE_ADMIN autenticado<br>**When** se envía `DELETE /api/v1/rules/{id}` con un id inexistente<br>**Then** el sistema retorna HTTP 404 | STORE_ADMIN autenticado; id de regla inexistente | id=nonExistentId (UUID válido no registrado) | 1. Enviar DELETE /api/v1/rules/{nonExistentId}<br>2. Verificar HTTP 404 | HTTP 404; regla no encontrada | Resultado obtenido: HTTP 404; regla inexistente no encontrada; Estado: Pasó | Crítico |
| HU-15 | TC-060 | **Given** el SUPER_ADMIN está autenticado<br>**When** se envía `POST /api/v1/ecommerces` en tres iteraciones: Iter 1 con nombre y slug únicos válidos; Iter 2 con slug ya registrado; Iter 3 con campo slug vacío<br>**Then** Iter 1 retorna HTTP 201 con ecommerce activo; Iter 2 retorna HTTP 409 indicando conflicto de slug; Iter 3 retorna HTTP 400 indicando campo obligatorio | SUPER_ADMIN autenticado; Iter 1: slug no existente; Iter 2: slug "tienda-a" ya registrado tras Iter 1; Iter 3: sin restricciones adicionales | Iter 1: { "nombre": "Tienda A", "slug": "tienda-a" }; Iter 2: { "nombre": "Tienda B", "slug": "tienda-a" } (slug duplicado); Iter 3: { "nombre": "Tienda B", "slug": "" } | 1. Iter 1: Enviar POST con datos válidos → verificar HTTP 201 y estado activo en GET /ecommerces/{id}<br>2. Iter 2: Enviar POST con slug duplicado → verificar HTTP 409 y mensaje de conflicto; sin segundo ecommerce creado<br>3. Iter 3: Enviar POST con slug vacío → verificar HTTP 400 y mensaje de campo obligatorio | Iter 1: HTTP 201; ecommerce registrado con estado activo; Iter 2: HTTP 409; mensaje de slug duplicado; sin segundo ecommerce creado; Iter 3: HTTP 400; mensaje indica slug requerido; sin ecommerce creado | Resultado obtenido: Iter 1: HTTP 201; ecommerce con status ACTIVE confirmado en GET; Iter 2: HTTP 409 slug duplicado; Iter 3: HTTP 400 slug vacío; Estado: Pasó | Crítico |
| HU-15 | TC-061 | **Given** existe un ecommerce activo con usuarios y API Keys asociadas<br>**When** se envía `PATCH /api/v1/ecommerces/{id}/status` con `{ "active": false }`<br>**Then** el sistema retorna HTTP 200; los tokens JWT de los usuarios vinculados quedan inválidos (HTTP 401) y las API Keys del ecommerce son rechazadas por el Motor (HTTP 401) | SUPER_ADMIN autenticado; ecommerce activo con usuarios y API Keys | id del ecommerce; body { "active": false } | 1. Desactivar ecommerce con PATCH<br>2. Verificar HTTP 200<br>3. Intentar login con usuario del ecommerce → HTTP 401<br>4. Enviar request al Motor con API Key del ecommerce → HTTP 401 | HTTP 200 en desactivación; HTTP 401 en login de usuario vinculado; HTTP 401 en Motor con API Key del ecommerce | Resultado obtenido: HTTP 200 en desactivación; HTTP 401 en login de usuario vinculado; HTTP 401 en Motor con API Key del ecommerce desactivado; Estado: Pasó | Crítico |
| HU-16 | TC-062 | **Given** el SUPER_ADMIN está autenticado<br>**When** se envía `GET /api/v1/ecommerces` sin filtro de ecommerce<br>**Then** el sistema retorna HTTP 200 con la lista global de todos los ecommerces registrados | SUPER_ADMIN autenticado; uno o más ecommerces registrados | header Authorization: Bearer <token SUPER_ADMIN> | 1. Enviar GET /api/v1/ecommerces<br>2. Verificar HTTP 200<br>3. Verificar que la lista contiene todos los ecommerces sin restricción | HTTP 200; lista global sin restricción de ecommerce_id | Resultado obtenido: HTTP 200; lista global con uid, name, slug y status por ecommerce; Estado: Pasó | Crítico |
| HU-16 | TC-063 | **Given** el SUPER_ADMIN está autenticado<br>**When** ejecuta cuatro operaciones: `POST /api/v1/users` para crear un STORE_ADMIN, `GET /api/v1/ecommerces`, `DELETE /api/v1/users/{id}` para eliminar un STORE_ADMIN y `PATCH /api/v1/users/{id}/ecommerce` para reasignar un usuario<br>**Then** las cuatro retornan HTTP 2xx sin restricción de ecommerce | SUPER_ADMIN autenticado; ecommerces y usuarios existentes | Cuatro operaciones sobre ecommerces y usuarios distintos | 1. Ejecutar cada operación con token SUPER_ADMIN<br>2. Verificar HTTP 2xx en cada caso | HTTP 201 en creación; HTTP 200 en listado; HTTP 204 en eliminación; HTTP 200 en reasignación; sin bloqueo por ecommerce_id | Resultado obtenido: HTTP 201 creación STORE_ADMIN; HTTP 200 listado ecommerces; HTTP 204 eliminación; HTTP 200 reasignación de ecommerce; Estado: Pasó | Crítico |
| HU-11 | TC-064 | **Carga sostenida:** el endpoint `POST /api/v1/engine/calculate` recibe tráfico concurrente representativo del volumen de producción esperado durante un período sostenido. Se verifica que el motor mantiene tiempos de respuesta dentro del umbral bajo condiciones normales de uso. | 50 usuarios virtuales; duración=5 minutos; rampa de subida=30 s | Payload estándar con carrito válido de 3 ítems; tope=20%; API Key válida | p95 del tiempo de respuesta; tasa de error HTTP; throughput (req/s) | p95 ≤ 500 ms; tasa de error < 1% | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Crítico |
| HU-11 | TC-065 | **Pico de carga:** el endpoint `POST /api/v1/engine/calculate` recibe un pico repentino de usuarios simultáneos para verificar que el motor no degrada por encima del umbral bajo condiciones de estrés breve. | Rampa: 5 VUs a 200 en 1 minuto; meseta 2 minutos a 200 VUs; bajada 30 s | Mismo payload estándar; API Key válida | p95 durante la meseta; tasa de error; latencia máxima | p95 ≤ 500 ms durante meseta; tasa de error < 1% | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Crítico |
| HU-11 | TC-066 | **Consistencia del tope bajo carga:** bajo carga concurrente, se verifica que el motor aplica correctamente el tope de descuento y no devuelve resultados inconsistentes cuando múltiples solicitudes se procesan al mismo tiempo. | 30 usuarios virtuales; duración=3 minutos | Payload: descuento acumulado=35%, tope=20%; subtotal=$200.00 | Consistencia del campo `descuentoAplicado` en respuestas; tasa de error; p95 | 100% de respuestas con `descuentoAplicado=20`; p95 ≤ 500 ms; tasa de error < 1% | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Alto |
| HU-10 | TC-067 | **Health check del Motor:** verificar que el servicio `loyalty-engine` está en línea y responde antes de ejecutar cualquier prueba automatizada sobre el endpoint de clasificación. | Motor de Descuentos desplegado en `http://localhost:8082` | GET http://localhost:8082/health (sin body ni auth) | 1. Enviar GET /health<br>2. Verificar HTTP 200<br>3. Verificar que el body indica estado operativo | HTTP 200; body con indicador de salud positivo (ej. "status": "UP"); tiempo de respuesta < 1 s | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Crítico |